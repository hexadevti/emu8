// driver/adc.h — desktop stub. *_audio.cpp #include this for adc_power_acquire(), which is only
// called under BOARD_AUDIO_DAC=1 (the internal-DAC path), compiled out on desktop. Exists only so
// the #include resolves.
#pragma once
