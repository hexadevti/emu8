// driver/dac.h — desktop stub. Only the BOARD_AUDIO_DAC=1 branch of speaker.cpp uses dac_*; on
// desktop speaker.cpp is replaced by src/desktop/audio_sdl.cpp. Exists only so the #include resolves.
#pragma once
