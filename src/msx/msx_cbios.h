// msx_cbios.h - the embedded C-BIOS array was removed; the MSX1 main BIOS is now loaded from the SD
// card (/roms/msx/cbios.rom) by loadBiosFromSD() in msx.cpp. Stub kept so existing #includes resolve.
#pragma once
